name: curation-tool-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
    - pillow
    - pydantic>=2.0
    - click
    - tqdm
    - pyyaml
    - httpx
    - websocket-client
    - gradio
    - pytest
